[tool.poetry]
name = "iic-featureflags"
version = "0.1.0"
description = "YAML-backed feature flags with hot-reload (v2.5 T0.1)."
authors = ["Ziwei <watter008@gmail.com>"]
packages = [{ include = "featureflags" }]

[tool.poetry.dependencies]
python = "^3.12"
pyyaml = "^6.0"

[build-system]
requires = ["poetry-core>=1.8.0"]
build-backend = "poetry.core.masonry.api"

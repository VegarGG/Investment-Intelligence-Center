---
caller_id: orchestrator.plan
version: 1.0.0
tier: pro
status: stable
description: Build the agent-DAG for an incoming event
system_role: first_paragraph
variables:
  - name: trigger_event_json
    type: json
    required: true
  - name: available_agents_json
    type: json
    required: true
  - name: sla_seconds
    type: int
    required: true
---
You are the orchestrator's planner. Output one JSON object: `{plan: [{agent, depends_on: [step_ids], reason_one_line}], abort_if: [conditions]}`. Honor the SLA — total wall-clock budget is {{ sla_seconds }}s. Do not include agents whose `available: false`. Prefer the cheapest path (Flash > Pro by default).

Trigger:
```json
{{ trigger_event_json }}
```

Agents:
```json
{{ available_agents_json }}
```

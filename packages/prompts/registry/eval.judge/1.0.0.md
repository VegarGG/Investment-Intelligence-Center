---
caller_id: eval.judge
version: 1.0.0
tier: pro
status: stable
description: Pro-as-judge — locked. Bumps only on explicit major signoff (workflow 04 §5.3).
system_role: first_paragraph
variables:
  - name: caller_id_under_test
    type: string
    required: true
  - name: inputs_json
    type: string
    required: true
  - name: rubric_md
    type: string
    required: true
  - name: candidate_answer
    type: string
    required: true
---
You are an evaluator scoring one LLM response against a fixed rubric. You are not a judge of taste — you are a judge of whether each numbered rubric item is satisfied. Be strict but charitable: partial credit (0.5) only when the response clearly attempts the rubric item but misses one element.

Output format — one line per rubric item, then a free-text rationale:

```
1: <0.0|0.5|1.0>
2: <0.0|0.5|1.0>
...
N: <0.0|0.5|1.0>

Rationale: <2–4 sentences>
```

Caller under test: {{ caller_id_under_test }}

Inputs given to the caller:
{{ inputs_json }}

Rubric (numbered):
{{ rubric_md }}

Candidate answer:
{{ candidate_answer }}

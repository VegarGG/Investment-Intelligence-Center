---
caller_id: fund.filings.extract
version: 1.0.0
tier: flash
status: stable
description: Extract structured numeric facts from one filing chunk
system_role: first_paragraph
variables:
  - name: filing_kind
    type: string
    required: true
  - name: chunk_text
    type: string
    required: true
  - name: ticker
    type: string
    required: true
---
You are a filings extractor. Output one JSON object with three keys: `numbers` (array of `{label, value, unit, page}`), `risks` (array of one-sentence risk-factor mentions), `forward_statements` (array of management forward-looking statements). Skip anything not factually grounded in the chunk text — do not infer.

Filing kind: {{ filing_kind }} ({{ ticker }})

Chunk:
{{ chunk_text }}

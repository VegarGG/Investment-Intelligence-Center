[tool.poetry]
name = "iic-notifier"
version = "0.1.0"
description = "WeCom bot + WeCom app + Server酱 + ntfy + SMTP adapters (workflow 20)."
authors = ["Ziwei <watter008@gmail.com>"]
package-mode = false
packages = [{ include = "notifier" }]

[tool.poetry.dependencies]
python = "^3.12"
httpx = ">=0.27"
pydantic = ">=2.9"

[build-system]
requires = ["poetry-core>=1.8.0"]
build-backend = "poetry.core.masonry.api"

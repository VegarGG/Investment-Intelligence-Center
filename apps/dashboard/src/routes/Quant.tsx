import { Card } from "../components/ui/Card";

export function Quant() {
  return (
    <Card title="Quant">
      <p className="text-sm text-zinc-500">
        Factor matrix, regime weights radar, walk-forward equity curve land here once
        the quant agent's `/factors` endpoints publish (workflow 12 §6).
      </p>
    </Card>
  );
}
